#include <test.hpp>

inline void REQUIRE_GEN_MIDDLE(TTacCell a, TTacCell b, TTacCell expect) {
  TTacCell result = MIDDLE(a, b);

  INFO("a: " << cell_name(a) << " [" << std::bitset<4>(a) << "]");
  INFO("b: " << cell_name(b) << " [" << std::bitset<4>(b) << "]");
  INFO("result: " << cell_name(result) << " [" << std::bitset<4>(result) << "]");
  INFO("expected: " << cell_name(expect) << " [" << std::bitset<4>(expect) << "]");

  REQUIRE(result == expect);
}

TEST_CASE("MIDDLE", "[GENERATE][MIDDLE]") {
  REQUIRE_GEN_MIDDLE(TTAC_TOP_LEFT, TTAC_TOP_RIGHT, TTAC_TOP);
  REQUIRE_GEN_MIDDLE(TTAC_TOP_RIGHT, TTAC_TOP_LEFT, TTAC_TOP);

  // Bottom edge
  REQUIRE_GEN_MIDDLE(TTAC_BOTTOM_LEFT, TTAC_BOTTOM_RIGHT, TTAC_BOTTOM);
  REQUIRE_GEN_MIDDLE(TTAC_BOTTOM_RIGHT, TTAC_BOTTOM_LEFT, TTAC_BOTTOM);

  // Left edge
  REQUIRE_GEN_MIDDLE(TTAC_TOP_LEFT, TTAC_BOTTOM_LEFT, TTAC_LEFT);
  REQUIRE_GEN_MIDDLE(TTAC_BOTTOM_LEFT, TTAC_TOP_LEFT, TTAC_LEFT);

  // Right edge
  REQUIRE_GEN_MIDDLE(TTAC_TOP_RIGHT, TTAC_BOTTOM_RIGHT, TTAC_RIGHT);
  REQUIRE_GEN_MIDDLE(TTAC_BOTTOM_RIGHT, TTAC_TOP_RIGHT, TTAC_RIGHT);

  REQUIRE_GEN_MIDDLE(TTAC_TOP, TTAC_LEFT, TTAC_TOP_LEFT);
  REQUIRE_GEN_MIDDLE(TTAC_LEFT, TTAC_TOP, TTAC_TOP_LEFT);

  // Top + Right = Top Right
  REQUIRE_GEN_MIDDLE(TTAC_TOP, TTAC_RIGHT, TTAC_TOP_RIGHT);
  REQUIRE_GEN_MIDDLE(TTAC_RIGHT, TTAC_TOP, TTAC_TOP_RIGHT);

  // Bottom + Left = Bottom Left
  REQUIRE_GEN_MIDDLE(TTAC_BOTTOM, TTAC_LEFT, TTAC_BOTTOM_LEFT);
  REQUIRE_GEN_MIDDLE(TTAC_LEFT, TTAC_BOTTOM, TTAC_BOTTOM_LEFT);

  // Bottom + Right = Bottom Right
  REQUIRE_GEN_MIDDLE(TTAC_BOTTOM, TTAC_RIGHT, TTAC_BOTTOM_RIGHT);
  REQUIRE_GEN_MIDDLE(TTAC_RIGHT, TTAC_BOTTOM, TTAC_BOTTOM_RIGHT);
}
